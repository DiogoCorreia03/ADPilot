# MCP Client Configuration
#
# Optional pentest phase header to send to the MCP session ('pentest_phase' extra_headers)
# Common values: "shell_only", "external_recon", "initial_access", "internal_recon", "lateral_privesc"
PENTEST_PHASE="shell_only"
